# JPA에서 get과 find의 네이밍 차이
---

## 기준

### 값을 가져오는 시간


### 데이터를 가져올 때 에러처리 여부

- `get`
	- 찾는 도중 값이 없으면 `null` 을 반환한다.
	- 비교적 복잡하지 않은 로직
	- JPA에 `getOne()` 의 경우 오류 발생함. 
- `find`
	- 찾는 도중 값이 없으면 오류를 발생시킨다.
	- 비교적 복잡한 로직
	- JPA에 `findById()`의 경우 오류 발생 없음 다만 반환 타입이 Optional 이기 때문인 것으로 보임
	


